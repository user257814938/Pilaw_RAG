export default function LoginPage() {
    return (
        <div className="flex flex-col items-center justify-center min-h-screen">
            <h1>Connexion / Inscription</h1>
            <p>Formulaire d'authentification ici (Email + Mot de passe ou OAuth)</p>
        </div>
    );
}
